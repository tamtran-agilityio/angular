import { FormatLinkPipe } from './format-link.pipe';

describe('FormatLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
