import { ControlMessagesModule } from './control-messages.module';

describe('ControlMessagesModule', () => {
  it('should work', () => {
    expect(new ControlMessagesModule()).toBeDefined();
  });
});
